# 환경 변수 (API 키)
.env

# 노트북 (토큰 포함 가능)
finetune/*.ipynb

# RAG 바이너리 데이터
RAG/
RAG.zip

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/

# 모델 파일 (바이너리 제외, Modelfile은 추적)
*.gguf
*.safetensors
*.bin
models/*
!models/Modelfile

# zip 파일
*.zip

# 평가 캐시
eval/__pycache__/
eval/*.zip

# Claude Code 로컬 권한 (사용자별 설정)
.claude/settings.local.json

# pytest-cov 산출물 (리포트 .md는 추적, 바이너리는 제외)
.coverage
.coverage.*
docs/coverage_html/
htmlcov/
